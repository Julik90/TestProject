package selenium.framework.browser.elements;

import org.openqa.selenium.By;

public class FormElement extends AbstractElement {
    public FormElement(String name, By locator) {
        super(name, locator);
    }
}
